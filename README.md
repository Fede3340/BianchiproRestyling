
  # App capabilities overview

  This is a code bundle for App capabilities overview. The original project is available at https://www.figma.com/design/brHz1ZOcch0lObtYaEuHEC/App-capabilities-overview.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  